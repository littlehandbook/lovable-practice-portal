
# Copy this file to .env and update with your actual values

# API Configuration
VITE_API_URL=https://your-microservice-domain.com

# Note: 
# - VITE_ prefix is required for Vite to expose the variable to client-side code
# - If not set, the app falls back to '/api' proxy for local development
# - Use .env.production for production-specific overrides
